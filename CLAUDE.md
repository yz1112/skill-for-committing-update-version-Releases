# Claude Assistant Rules
Whenever the user requests a version bump, a release, or to push updates (e.g. "ارفع التحديثات", "اعمل ريليس"), you must read the `SKILL.md` file in the root directory and execute the instructions found there exactly. The workflow requires terminal commands to bump versions, create git tags, and GitHub releases.
